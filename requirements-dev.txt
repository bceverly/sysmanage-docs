# Development / lint tooling for the sysmanage-docs repo.
# Installed by ``make install-dev``; used by ``make lint`` (lint-python +
# lint-security).  Versions floored to match the sibling SysManage repos.
pylint>=3.0.0
bandit>=1.8.6
