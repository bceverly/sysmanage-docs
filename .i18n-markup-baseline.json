{
  "_comment": "Pre-existing markup mismatches, recorded so the gate fails on NEW ones. A ratchet, not an amnesty: this list may only shrink. Fix with `python3 scripts/i18n_check_markup.py --requeue`, then `make translate`, then `--prune`.",
  "known": []
}
