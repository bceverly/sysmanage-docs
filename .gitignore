*.bak
*.bak2
typos
node_modules/*
package-lock.json
.DS_Store

# Python build artifacts (from scripts/*.py helpers)
__pycache__/
*.py[cod]
*$py.class

# Claude Code workspace
.claude/

# Package repository — published straight to Cloudflare R2 (https://repo.sysmanage.org)
# by the sysmanage / sysmanage-agent release pipelines and pulled locally only
# as a staging area for `make prune-repo`.  NOT tracked in git (was ~1.2GB and
# growing unbounded across releases).
repo/
